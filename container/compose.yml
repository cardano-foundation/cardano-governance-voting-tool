services:
  app:
    build:
      context: ..
      dockerfile: container/Dockerfile
    ports:
      - "8000:8000"
    environment:
      - IPFS_PRECONFIGS_JSON=${IPFS_PRECONFIGS_JSON}
      - NETWORK_ID=${NETWORK_ID}
